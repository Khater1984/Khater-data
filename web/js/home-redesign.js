/* Home V4: single navigation surface, data-bound market snapshot, responsive controls. */
(function(window){'use strict';
const defs=[['USD / EGP','usd_egp_mid','system'],['EGX30','egx30_close','equity'],['Gold / EGP','gold_egp_oz','hedge'],['Silver / EGP','silver_egp_oz','hedge'],['S&P 500 / EGP','spy_egp','global'],['NASDAQ 100 / EGP','qqq_egp','global'],['BTC / EGP','btc_egp','risk']];
const fmt=(n,d=2)=>n==null||!Number.isFinite(Number(n))?'—':Number(n).toLocaleString('en-US',{maximumFractionDigits:d});
const last=s=>s&&Array.isArray(s.rows)&&s.rows.length?s.rows[s.rows.length-1]:null;
const change=(rows,days=30)=>{if(!Array.isArray(rows)||rows.length<2)return null;const latest=rows[rows.length-1],lv=Number(latest.value);if(!Number.isFinite(lv))return null;const cutoff=new Date(latest.ts_date+'T00:00:00');cutoff.setDate(cutoff.getDate()-days);for(let i=rows.length-1;i>=0;i--){const r=rows[i],v=Number(r.value),d=new Date(r.ts_date+'T00:00:00');if(d<=cutoff&&Number.isFinite(v)&&v!==0)return(lv-v)/v*100}return null};
const tone=v=>v==null?'':v>0?'up':v<0?'dn':'';const signed=v=>v==null?'—':`${v>0?'+':''}${fmt(v)}%`;
function row(r){const c=change(r.series?.rows);return `<div class="pulse-row" data-value="${r.value??''}" data-change="${c??''}" data-index="${r.index}" data-direction="${c>0?'up':c<0?'dn':'flat'}"><span class="pulse-name"><i class="pulse-group ${r.group}"></i>${r.label}</span><b>${fmt(r.value)}</b><strong class="${tone(c)}">${signed(c)}</strong><small>${r.date||'—'}</small><span class="trend-slot" data-label="${r.label}"></span></div>`}
function leader(r){const c=change(r.series?.rows);return `<a class="leader-card ${r.group}" href="./macro.html"><div class="leader-top"><span>${r.label}</span><em>${r.group.toUpperCase()}</em></div><b>${fmt(r.value)}</b><strong class="${tone(c)}">${signed(c)} · 30D</strong><div class="leader-chart trend-slot" data-label="${r.label}"></div><small>${r.date||'—'}</small></a>`}
let currentSort='default';
function controls(){const host=document.getElementById('market-pulse');if(!host||host.querySelector('.market-toolbar'))return;const t=document.createElement('div');t.className='market-toolbar';t.innerHTML='<span class="market-toolbar-label">السوق</span><button class="market-sort active" data-sort="default">الأساسي</button><button class="market-sort" data-sort="change-desc">الأعلى</button><button class="market-sort" data-sort="change-asc">الأدنى</button><button class="market-sort" data-sort="value">القيمة</button><span class="market-toolbar-label">الاتجاه</span><button class="market-filter active" data-filter="all">الكل</button><button class="market-filter" data-filter="up">صاعد</button><button class="market-filter" data-filter="dn">هابط</button>';t.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.filter){t.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));sortRows(currentSort,b.dataset.filter);return}if(b.dataset.sort){currentSort=b.dataset.sort;t.querySelectorAll('[data-sort]').forEach(x=>x.classList.toggle('active',x===b));const f=t.querySelector('[data-filter].active')?.dataset.filter||'all';sortRows(currentSort,f)}});host.insertBefore(t,host.firstChild)}
function sortRows(mode='default',filter='all'){const host=document.getElementById('market-pulse');if(!host)return;const rows=[...host.querySelectorAll('.pulse-row:not(.pulse-head)')];rows.forEach(x=>x.hidden=filter!=='all'&&x.dataset.direction!==filter);const visible=rows.filter(x=>!x.hidden);visible.sort((a,b)=>{if(mode==='change-desc')return Number(b.dataset.change||-Infinity)-Number(a.dataset.change||-Infinity);if(mode==='change-asc')return Number(a.dataset.change||Infinity)-Number(b.dataset.change||Infinity);if(mode==='value')return Number(b.dataset.value||-Infinity)-Number(a.dataset.value||-Infinity);return Number(a.dataset.index)-Number(b.dataset.index)});visible.forEach(x=>host.appendChild(x))}
function derive(ch,infl,tbill){const{egx,gold,usd}=ch;if(egx==null||gold==null||usd==null)return['السياق قيد الاكتمال','إشارة أو أكثر لم تصل بعد إلى طبقة البيانات المشتركة.'];if(gold>2&&usd>0)return['تحوّط نقدي / ذهب حاضر في المشهد','ارتفاع الذهب مع تحرك الدولار يجعل حماية القوة الشرائية جزءًا مهمًا من القراءة قبل مقارنة الصناديق.'];if(tbill!=null&&infl!=null&&tbill>=infl)return['العائد النقدي يفرض خط أساس مرتفعًا',`العائد النقدي (${fmt(tbill)}%) يتطلب من أي مخاطرة إضافية أن تقدم تفوقًا واضحًا.`];if(egx>0&&gold>0)return['زخم متعدد الأصول مع عائد نقدي مهم','الأسهم والذهب يتحركان إيجابيًا؛ المقارنة مع خط الأساس النقدي أهم من مطاردة اتجاه واحد.'];return['سوق متباين — المقارنة أهم من الاتجاه','لا يوجد اتجاه واحد يختصر المشهد الحالي.']}
async function render(){try{const series=await Promise.all(defs.map(async([label,key,group],index)=>{let s=null;try{s=await window.KHATER_DATA.macro.getSeries(key)}catch(e){}const l=last(s);return{label,key,group,index,series:s,value:l?.value??null,date:l?.ts_date??null}}));const extra=await Promise.all([window.KHATER_DATA.macro.getSeries('cpi_headline_mom_pct').catch(()=>null),window.KHATER_DATA.macro.getSeries('tbill_364_avg_yield_pct').catch(()=>null)]);const map=Object.fromEntries(series.map(r=>[r.label,r]));const ch={usd:change(map['USD / EGP'].series?.rows),egx:change(map['EGX30'].series?.rows),gold:change(map['Gold / EGP'].series?.rows)};const[infl,tbill]=extra.map(last);const d=derive(ch,infl?.value,tbill?.value);document.getElementById('regime-title').textContent=d[0];document.getElementById('regime-copy').textContent=d[1];const latest=series.map(r=>r.date).filter(Boolean).sort().pop()||'—';document.getElementById('terminal-stamp').textContent=`آخر مشاهدة · ${latest}`;const leaders=document.getElementById('market-leaders');leaders.innerHTML=series.filter(r=>r.value!=null).slice(0,3).map(leader).join('');const pulse=document.getElementById('market-pulse');pulse.innerHTML='<div class="pulse-row pulse-head"><span>المؤشر</span><span>القيمة</span><span>30 يوم</span><span>المشاهدة</span><span>الاتجاه</span></div>'+series.map(row).join('');controls();if(window.KHATER_CHARTS?.boot)setTimeout(()=>window.KHATER_CHARTS.boot(),20)}catch(e){console.error('[home-redesign]',e)}}
window.KHATER_HOME_V4={render};document.addEventListener('DOMContentLoaded',()=>setTimeout(render,30),{once:true});
})(window);
