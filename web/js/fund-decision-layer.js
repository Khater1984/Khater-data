/* Decision layer: connects existing Fund DNA evidence to a concise investor reading. */
(function(window){
'use strict';
function esc(v){return window.FUND&&FUND.esc?FUND.esc(v):String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));}
function scoreClass(v){v=Number(v);return v>=75?'good':v>=50?'mid':v>0?'weak':'none';}
function render(d){const host=document.getElementById('decision-layer');if(!host)return;const f=d.fund||{},s=d.score||{},p=d.performance||{};const score=Number(s.final_score);const h=new URLSearchParams(location.search).get('h')||'last12m';const rows=(d.officialSeriesByHorizon&&d.officialSeriesByHorizon[h])||[];const last=rows[rows.length-1]||{};const ret=Number(last.return_pct);const rating=s.rating||'NOT RATED';let verdict='يحتاج قراءة متأنية',tone='neutral';if(Number.isFinite(score)&&score>=75&&Number.isFinite(ret)&&ret>0){verdict='مرشح قوي ضمن سياقه';tone='good'}else if(Number.isFinite(score)&&score<50){verdict='إشارة حذر';tone='warn'}else if(Number.isFinite(ret)&&ret<0){verdict='العائد الحالي يحتاج تفسيرًا';tone='warn'}host.innerHTML='<section class="decision-card '+tone+'"><div class="decision-kicker">DECISION INTELLIGENCE</div><div class="decision-main"><div><h2>'+esc(verdict)+'</h2><p>هذه قراءة تجميعية من البيانات المحفوظة للصندوق، وليست توصية شراء أو بيع.</p></div><div class="decision-score '+scoreClass(score)+'"><span>SmartScore</span><b>'+(Number.isFinite(score)?score.toFixed(1):'—')+'</b><small>'+esc(rating)+'</small></div></div><div class="decision-grid"><div><span>السياق</span><b>'+esc(f.category||'—')+'</b><small>الفئة المسجلة</small></div><div><span>الأداء</span><b>'+(Number.isFinite(ret)?(ret>=0?'+':'')+ret.toFixed(2)+'%':'—')+'</b><small>'+esc(h)+'</small></div><div><span>الثقة</span><b>'+esc(s.data_confidence||'—')+'</b><small>'+esc(s.data_quality||'')+'</small></div><div><span>التأهل</span><b>'+esc(s.qualification_status||'—')+'</b><small>Track Factor: '+(s.track_factor==null?'—':Number(s.track_factor).toFixed(2))+'</small></div></div><div class="decision-path"><span>Why?</span><b>Performance</b><i>→</i><b>Benchmark</b><i>→</i><b>Quality</b><i>→</i><b>Reliability</b></div></section>'}
window.FUND_DECISION_LAYER={render};
})(window);
