(function () {
  'use strict';
  const F = window.FUND;
  const S = window.KHATER_DATA && window.KHATER_DATA.fund;
  const PERIODS = ['weekly','4weeks','ytd','last12m','1y','2y','3y','4y','5y','6y'];
  function selectedHorizon() { const h = new URLSearchParams(location.search).get('h') || 'last12m'; return PERIODS.indexOf(h) >= 0 ? h : 'last12m'; }
  function n(v) { if (v == null || String(v).trim() === '') return null; const x = Number(v); return Number.isFinite(x) ? x : null; }
  function setHorizon(h) { const u = new URL(location.href); u.searchParams.set('id', F.id); u.searchParams.set('h', h); history.replaceState({}, '', u); render(); }
  function selectedPerformanceRows(horizon) { return S.performanceSeries(F.performance || [], horizon); }
  function validate(rows, horizon) {
    if (!rows.length) return 'لا توجد سجلات أداء رسمية صالحة لهذا الأفق.';
    if (rows.some(r => String(r.horizon) !== horizon || !r.report_date || n(r.return_pct) == null)) return 'Performance data integrity check failed: horizon mismatch.';
    for (let i=1;i<rows.length;i++) if (String(rows[i-1].report_date) >= String(rows[i].report_date)) return 'Performance data integrity check failed: dates are not strictly ascending.';
    return null;
  }
  function svgChart(rows) { const w=820,h=300,p=32,vals=rows.map(r=>n(r.return_pct)); let mn=Math.min(...vals),mx=Math.max(...vals); if(mn===mx){mn-=1;mx+=1;} const range=mx-mn,X=i=>rows.length===1?w/2:p+i*(w-2*p)/(rows.length-1),Y=v=>h-p-((v-mn)/range)*(h-2*p); const line=rows.map((r,i)=>X(i).toFixed(2)+','+Y(Number(r.return_pct)).toFixed(2)).join(' '),area=p+','+(h-p)+' '+line+' '+X(rows.length-1).toFixed(2)+','+(h-p); const hits=rows.map((r,i)=>'<circle class="chart-hit" data-i="'+i+'" cx="'+X(i).toFixed(2)+'" cy="'+Y(Number(r.return_pct)).toFixed(2)+'" r="10" fill="transparent"></circle>').join(''); return '<svg viewBox="0 0 '+w+' '+h+'" preserveAspectRatio="none" role="img" aria-label="Official rolling return history"><line class="axis" x1="0" y1="'+Y(0).toFixed(2)+'" x2="'+w+'" y2="'+Y(0).toFixed(2)+'"/><polyline class="chart-fill" points="'+area+'"/><polyline class="chart-line" points="'+line+'"/>'+hits+'</svg>'; }
  function bindTooltip(host,rows){const wrap=host.querySelector('.chart-wrap'),tip=host.querySelector('.chart-tip');if(!wrap||!tip)return;wrap.querySelectorAll('.chart-hit').forEach(el=>el.addEventListener('mouseenter',e=>{const r=rows[Number(el.dataset.i)];tip.hidden=false;tip.innerHTML='<b>'+F.esc(r.report_date)+'</b><span>العائد الرسمي '+F.pct(r.return_pct)+'</span><span>الأفق: '+F.esc(F.L[r.horizon]||r.horizon)+'</span><span>المصدر: fund_performance_history</span>';const box=wrap.getBoundingClientRect();tip.style.left=Math.min(Math.max(12,e.clientX-box.left+12),Math.max(12,box.width-220))+'px';tip.style.top=Math.max(8,e.clientY-box.top-90)+'px';}));wrap.addEventListener('mouseleave',()=>{tip.hidden=true;});}
  function navSnapshot(){const prices=(F.prices||[]).filter(x=>x&&x.as_of_date&&n(x.nav)>0).sort((a,b)=>String(a.as_of_date).localeCompare(String(b.as_of_date)));return {latest:prices[prices.length-1],previous:prices[prices.length-2]};}
  function render(){
    const host=document.getElementById('performance-tab');if(!host)return;const horizon=selectedHorizon(),rows=selectedPerformanceRows(horizon),error=validate(rows,horizon),first=rows[0],latest=rows[rows.length-1],nav=navSnapshot();
    const tabs=PERIODS.map(x=>'<button type="button" data-h="'+x+'" aria-pressed="'+(x===horizon?'true':'false')+'" class="'+(x===horizon?'active':'')+'">'+F.esc(F.L[x]||x)+'</button>').join('');
    if(error){host.innerHTML='<div class="tab-nav perf-horizons">'+tabs+'</div><div class="empty integrity-error"><strong>Performance data integrity check failed</strong><p>'+F.esc(error)+'</p><p>لم يتم عرض رسم أو مؤشرات متضاربة.</p></div>';}
    else{const navChange=nav.latest&&nav.previous?((Number(nav.latest.nav)/Number(nav.previous.nav)-1)*100):null;host.innerHTML='<div class="pf-shell"><div class="tab-nav perf-horizons" id="perf-horizons">'+tabs+'</div><div class="perf-strip"><div><span>OFFICIAL RETURN</span><strong>'+F.pct(latest.return_pct)+'</strong><small>'+F.esc(latest.report_date)+'</small></div><div><span>FIRST OFFICIAL RETURN</span><strong>'+F.pct(first.return_pct)+'</strong><small>'+F.esc(first.report_date)+'</small></div><div><span>PERFORMANCE RECORDS</span><strong>'+rows.length+'</strong><small>لأفق '+F.esc(F.L[horizon])+'</small></div><div><span>LATEST NAV · منفصل</span><strong>'+F.num(nav.latest&&nav.latest.nav)+'</strong><small>'+F.esc(nav.latest&&nav.latest.as_of_date||'غير متاح')+'</small></div></div><div class="perf-grid"><div class="card chart-card"><div class="chart-top"><div><span class="perf-kicker">OFFICIAL PERFORMANCE HISTORY</span><h3>السجل التاريخي للعائد المتحرك — '+F.esc(F.L[horizon])+'</h3></div><strong>'+F.pct(latest.return_pct)+'</strong></div><div class="chart-wrap">'+svgChart(rows)+'<div class="chart-tip" hidden></div></div><div class="perf-legend"><span><i></i>العائد الرسمي</span><span>من '+F.esc(first.report_date)+' إلى '+F.esc(latest.report_date)+'</span><span>المصدر: fund_performance_history</span></div><p class="note">كل نقطة في الرسم هي صف رسمي مستقل لنفس الأفق. لا يتم إعادة حساب العائد من NAV ولا خلط أي أفق آخر.</p></div><div class="card table-card"><div class="record-head"><div><span class="perf-kicker">PERFORMANCE RECORD</span><h3>'+F.esc(F.L[horizon])+'</h3><small>نفس السلسلة المستخدمة في الرسم والمؤشرات</small></div><b>'+rows.length+' سجل</b></div><div class="pf-official"><div class="pf-official-grid"><div><span>العائد الرسمي الأحدث</span><strong>'+F.pct(latest.return_pct)+'</strong><small>'+F.esc(latest.report_date)+'</small></div><div><span>أول عائد رسمي</span><strong>'+F.pct(first.return_pct)+'</strong><small>'+F.esc(first.report_date)+'</small></div><div><span>أحدث NAV · منفصل</span><strong>'+F.num(nav.latest&&nav.latest.nav)+'</strong><small>'+F.esc(nav.latest&&nav.latest.as_of_date||'غير متاح')+'</small></div><div><span>تغير NAV الأخير</span><strong>'+F.pct(navChange)+'</strong><small>ليس عائد الأداء الرسمي</small></div></div></div><div class="table-scroll"><table><thead><tr><th>التاريخ</th><th>العائد الرسمي</th><th>NAV عند التقرير</th><th>المصدر</th></tr></thead><tbody>'+rows.slice().reverse().map(r=>'<tr><td>'+F.esc(r.report_date)+'</td><td class="num">'+F.pct(r.return_pct)+'</td><td class="num">'+F.num(r.nav_value)+'</td><td>'+F.esc(r.source_id||'fund_performance_history')+'</td></tr>').join('')+'</tbody></table></div></div></div><div class="pf-footnote">مصدر الحقيقة الوحيد لهذا القسم: fund_performance_history بعد فلترة fund_id والأفق '+F.esc(horizon)+'. الرسم، Official Return، Tooltip، Performance Record، وعدد السجلات مشتقة من نفس الصفوف دون حسابات بديلة.</div></div>';bindTooltip(host,rows);}
    host.querySelectorAll('#perf-horizons button,.perf-horizons button').forEach(btn=>btn.addEventListener('click',()=>setHorizon(btn.dataset.h)));
    const heroRet=document.querySelector('.pulse .metric:nth-child(2) .metric-value'),heroNote=document.querySelector('.pulse .metric:nth-child(2) .note'),heroMuted=document.querySelector('.pulse .metric:nth-child(2) .muted');if(heroRet){heroRet.textContent=error?'—':F.pct(latest.return_pct);heroNote.textContent=error?'بيانات رسمية غير مكتملة':latest.report_date;heroMuted.textContent='OFFICIAL RETURN · '+(F.L[horizon]||horizon);}
  }
  window.addEventListener('popstate',render);window.FUND_TABS=window.FUND_TABS||{};window.FUND_TABS.performance=render;
})();
