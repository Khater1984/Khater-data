(function(){
  'use strict';
  const F=window.FUND;
  const B=[
    {label:'التضخم',series:'cpi_headline_mom_pct',type:'inflation',icon:'CPI'},
    {label:'أذون الخزانة · 364 يوم',series:'tbill_364_avg_yield_pct',type:'tbill',icon:'T-BILL'},
    {label:'بيتكوين / جنيه',series:'btc_egp',type:'level',icon:'BTC'},
    {label:'ناسداك / جنيه',series:'qqq_egp',type:'level',icon:'NASDAQ'},
    {label:'S&P 500 / جنيه',series:'spy_egp',type:'level',icon:'S&P'},
    {label:'الدولار / جنيه',series:'usd_egp_mid',type:'level',icon:'FX'},
    {label:'الفضة / جنيه',series:'silver_egp_oz',type:'level',icon:'SILVER'},
    {label:'الذهب / جنيه',series:'gold_egp_oz',type:'level',icon:'GOLD'},
    {label:'EGX30',series:'egx30_close',type:'level',icon:'EGX'}
  ];
  function start(end,h){const d=new Date(end+'T00:00:00Z');if(h==='weekly')d.setUTCDate(d.getUTCDate()-7);else if(h==='4weeks')d.setUTCDate(d.getUTCDate()-28);else if(h==='ytd')d.setUTCMonth(0,1);else if(h==='last12m')d.setUTCFullYear(d.getUTCFullYear()-1);else if(/^\dy$/.test(h))d.setUTCFullYear(d.getUTCFullYear()-Number(h[0]));else return null;return d.toISOString().slice(0,10)}
  async function calc(b,end,h){const rs=start(end,h);if(!rs||(b.type==='inflation'&&(h==='weekly'||h==='4weeks')))return null;const p=await F.get('/rest/v1/macro_series?select=ts_date,value,unit&series_key=eq.'+encodeURIComponent(b.series)+'&ts_date=gte.'+rs+'&ts_date=lte.'+end+'&order=ts_date.asc&limit=5000');const v=p.filter(x=>x.value!=null);if(!v.length)return null;const a=v[0].ts_date,z=v.at(-1).ts_date;if(b.type==='level'){const x=Number(v[0].value),y=Number(v.at(-1).value);return x?{value:(y/x-1)*100,start:a,end:z,estimated:false}:null}if(b.type==='inflation'){let f=1;for(const x of v){const n=Number(x.value);if(!Number.isFinite(n))return null;f*=1+n/100}return{value:(f-1)*100,start:a,end:z,estimated:false}}const vals=v.map(x=>Number(x.value));if(vals.some(x=>!Number.isFinite(x)))return null;const avg=vals.reduce((x,y)=>x+y,0)/vals.length,days=Math.max(1,(Date.parse(z)-Date.parse(a))/86400000);return{value:avg*days/365,start:a,end:z,estimated:true}}
  function row(item,ret,maxAbs){const v=item.o?.value!=null?Number(item.o.value):null,d=ret!=null&&v!=null?ret-v:null,pct=v==null?0:Math.max(3,Math.min(100,Math.abs(v)/Math.max(1,maxAbs)*100)),cls=v==null?'na':v>=0?'up':'down';return '<article class="bm-row '+cls+'"><div class="bm-name"><span class="bm-icon">'+item.b.icon+'</span><div><strong>'+F.esc(item.b.label)+'</strong><small>'+F.esc(item.o?(item.o.start+' → '+item.o.end):'لا توجد مشاهدة متاحة')+(item.o?.estimated?' · محسوب':'')+'</small></div></div><div class="bm-bar"><i style="width:'+pct+'%"></i></div><strong class="bm-value">'+F.pct(v)+'</strong><span class="bm-gap">'+(d==null?'—':(d>=0?'تفوق الصندوق ':'تقدم المؤشر ')+F.pct(Math.abs(d)))+'</span></article>'}
  async function render(){const host=document.getElementById('benchmark-tab');let h=new URLSearchParams(location.search).get('h')||'last12m';if(!F.HS.includes(h))h='last12m';const sel=(F.performance||[]).find(x=>x.horizon===h)||null,ret=sel&&sel.return_pct!=null?Number(sel.return_pct):null,end=sel&&sel.report_date||null;const out=end?await Promise.all(B.map(async b=>{try{return{b,o:await calc(b,end,h)}}catch(e){return{b,o:null}}})):B.map(b=>({b,o:null}));const valid=out.filter(x=>x.o?.value!=null),maxAbs=Math.max(1,...valid.map(x=>Math.abs(Number(x.o.value)))),wins=valid.filter(x=>ret!=null&&ret>=Number(x.o.value)).length,top=valid.slice().sort((a,b)=>Number(b.o.value)-Number(a.o.value))[0];host.innerHTML='<section class="bm-shell"><header class="bm-hero"><div><span class="bm-kicker">MARKET CONTEXT · '+F.esc(F.L[h]||h)+'</span><h2>أين يقف الصندوق أمام السوق؟</h2><p>مقارنة العائد خلال نفس الفترة مع التضخم، أدوات الدخل الثابت، والأسواق والأصول المرجعية.</p></div><div class="bm-fund-return"><small>عائد الصندوق</small><strong>'+F.pct(ret)+'</strong><span>'+F.esc(end||'لا يوجد تاريخ')+'</span></div></header><div class="bm-summary"><div><span>مقارنات متاحة</span><strong>'+valid.length+' / '+B.length+'</strong></div><div><span>تفوق على</span><strong class="bm-positive">'+wins+'</strong><small>من المؤشرات المتاحة</small></div><div><span>أعلى مرجع</span><strong>'+F.pct(top?.o?.value)+'</strong><small>'+F.esc(top?.b?.label||'غير متاح')+'</small></div><div><span>قراءة الفترة</span><strong class="'+(ret==null?'':'bm-positive')+'">'+(ret==null?'—':(ret>=0?'إيجابية':'سالبة'))+'</strong><small>'+F.esc(F.L[h]||h)+'</small></div></div><div class="bm-head"><div><h3>لوحة المقارنة</h3><span>القيمة = عائد المرجع · الشريط = الحجم النسبي · التفوق = عائد الصندوق ناقص المرجع</span></div><span class="bm-legend"><i class="up"></i>موجب <i class="down"></i>سالب</span></div><div class="bm-list">'+out.map(x=>row(x,ret,maxAbs)).join('')+'</div><footer class="bm-note">تستخدم المقارنة المشاهدات الفعلية المتاحة في قاعدة البيانات. عبارة «محسوب» تعني أن المرجع احتُسب من السلسلة المتاحة، ولا تعني أنه عائد رسمي منشور.</footer></section>';}
  window.FUND_TABS=window.FUND_TABS||{};window.FUND_TABS.benchmark=render;
})();
