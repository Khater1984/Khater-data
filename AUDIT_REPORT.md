# تقرير التدقيق الشامل لمنصة رادار السوق المصري

**النطاق:** المستودع `Khater1984/Khater-data`، الواجهة المنشورة على GitHub Pages، وتكامل Supabase المرتبط بالمشروع.  
**نوع التدقيق:** مراجعة ساكنة للكود، فحوصات تشغيل محلية، فحص مباشر للواجهة، والتحقق من REST API قراءة فقط.  
**حالة التغييرات:** لم تُجرَ أي تعديلات على ملفات المشروع أثناء هذا التدقيق.

## الخلاصة التنفيذية

المنصة الحالية هي **نموذج أولي وظيفي ناجح جزئيًا** وليست بعدُ منتجًا واجهيًا متماسكًا قابلًا للتوسع. الصفحة الرئيسية وصفحة الاقتصاد وصفحة الصناديق تستطيع تحميل البيانات من Supabase، لكن البنية تعتمد على صفحات HTML مستقلة مع JavaScript مضمّن ومكرر، وملفات JavaScript مشتركة جزئية، ومصدرين متوازيين للبيانات: Supabase وملفات JSON ثابتة داخل `web/data`.

أعلى المخاطر ليست جمالية فقط. ملف `web/heatmap.html` فارغ تقريبًا، ولا توجد اختبارات آلية أو خطوة بناء تفحص الواجهة قبل النشر، كما أن جامع NAV يعطّل التحقق من شهادات TLS في عدة مواضع. ويؤدي اعتماد الواجهة على عدد كبير من طلبات REST المباشرة إلى حساسية واضحة للبطء، والكاش، وتغير مخطط البيانات.

التوصية المركزية هي تنفيذ **إعادة تأسيس تدريجية** بدل إضافة ترقيعات أخرى. تبدأ العملية بتثبيت عقد البيانات ومصدر حقيقة واحد، ثم استخراج طبقة Supabase مشتركة ونظام تصميم مشترك، وبعد ذلك نقل الصفحات واحدةً تلو الأخرى إلى مكونات قابلة للاختبار. يمكن إبقاء GitHub Pages وSupabase خلال الانتقال دون إعادة بناء البيانات من الصفر.

## مستوى المخاطر

| المستوى | المعنى | عدد الملاحظات الرئيسية |
|---|---|---:|
| حرج | وظيفة أساسية غير موجودة أو مسار قد يفشل بالكامل | 1 |
| عالٍ | يؤثر في الاعتمادية أو الأمان أو قابلية التوسع | 5 |
| متوسط | يرفع تكلفة الصيانة أو يضعف التجربة | 8 |
| منخفض | تحسينات جودة أو اتساق يمكن جدولتها لاحقًا | 5 |

## خريطة النظام الحالي

| الطبقة | الوضع الحالي | الملاحظة |
|---|---|---|
| العرض | 8 ملفات HTML ثابتة | كل صفحة تملك بنية وسلوكًا جزئيًا مستقلًا |
| JavaScript | 12 ملفًا، مع JavaScript مضمّن داخل عدة صفحات | تكرار وغياب حدود واضحة بين العرض والبيانات |
| CSS | 4 ملفات، مع CSS مضمّن كبير داخل صفحات رئيسية | يوجد نظام مشترك جزئي، لكنه ليس مصدرًا وحيدًا للتصميم |
| البيانات | Supabase REST + 4 ملفات JSON ثابتة | مصدران للحقيقة قد يختلفان زمنيًا ومضمونيًا |
| الإدخال | 6 سكربتات Python | جلب HTML خارجي وعمليات upsert مباشرة إلى Supabase |
| قاعدة البيانات | جداول وViews/دوال Supabase | متاحة للقراءة العامة من الواجهة، مع اعتماد مباشر على أسماء الحقول |
| النشر | GitHub Pages عبر workflow واحد حاليًا | النشر ناجح، لكن لا توجد بوابة اختبارات قبل النشر |
| الاختبارات | غير موجودة | لا توجد unit أو integration أو browser tests |

## النتائج التفصيلية

### أولوية حرجة

#### ملف خريطة الحرارة فارغ فعليًا

الملف `web/heatmap.html` حجمه **بايت واحد فقط**. وجود رابط أو صفحة بهذا الاسم دون محتوى يعني أن جزءًا معلنًا من بنية المنتج غير منفذ أو يعرض صفحة فارغة. هذه ليست مشكلة تجميلية؛ إنها وظيفة مفقودة في المسار الأساسي للمنصة.

**الأثر:** تجربة مكسورة، فقدان ثقة، ورابط لا يحقق الوعد الموجود في التنقل أو تاريخ المشروع.  
**الإجراء المقترح:** تحديد ما إذا كانت خريطة الحرارة ستُدمج في `categories.html` أو ستصبح صفحة مستقلة. بعد تحديد القرار، يجب إما تنفيذها فعليًا أو إزالة الرابط والملف من المنتج إلى حين جاهزيتها.

### أولويات عالية

#### مصدران متوازيان للحقيقة

تحتوي المنصة على `web/data/engine_data.json` بحجم يقارب 780 كيلوبايت و`web/data/funds_dna.json` بحجم يقارب 152 كيلوبايت، وفي الوقت نفسه تقرأ صفحات مختلفة مباشرة من جداول Supabase مثل `funds` و`fund_performance_history` و`fund_smartscore_latest` و`macro_series`. كما يظهر `web/js/live.js` وهو يقرأ كلًا من Supabase وملفات JSON.

**الأثر:** قد يرى المستخدم بيانات مختلفة حسب الصفحة، وقد لا يتطابق تاريخ التحديث بين النسخة الثابتة والنسخة الحية.  
**الإجراء المقترح:** اعتماد Supabase كمصدر حقيقة للبيانات التشغيلية. تُستخدم ملفات JSON فقط كنسخة build محددة الغرض أو fallback معلن، مع وضع تاريخ snapshot ومصدره داخل البيانات نفسها.

#### لا توجد بوابة جودة قبل النشر

Workflow النشر في `.github/workflows/pages.yml` يرفع مجلد `web` مباشرة إلى GitHub Pages. لا توجد خطوة لفحص JavaScript، أو الروابط، أو JSON، أو التشغيل في متصفح، أو صحة عقود Supabase قبل النشر.

**الأثر:** يمكن دفع صفحة فارغة أو JavaScript مكسور إلى الإنتاج، ولا يُكتشف الخلل إلا بعد أن يراه المستخدم.  
**الإجراء المقترح:** إضافة workflow للتحقق يتضمن `node --check` لكل ملفات JavaScript، وJSON validation، وفحص الموارد المحلية، واختبار متصفح smoke لست صفحات رئيسية، ثم جعل النشر يعتمد على نجاح التحقق.

#### غياب الاختبارات الآلية بالكامل

لم توجد ملفات `package.json` أو `pytest.ini` أو إعدادات Playwright/Vitest أو ملفات اختبار. فحص Python الحالي يقتصر عمليًا على compile، وفحص JSON يدوي.

**الأثر:** أي تغيير في pagination أو الفرز أو الحسابات أو حالات البيانات الناقصة قد يعيد أخطاء سابقة دون إنذار.  
**الإجراء المقترح:** البدء باختبارات صغيرة للدوال الحسابية ومطابقة الصناديق، ثم اختبارات تكامل لعقود REST، ثم اختبارات متصفح للتحميل والفشل والفلاتر والروابط.

#### تعطيل تحقق TLS في جامع NAV

يستخدم `scripts/ingest_nav.py` الخيار `verify=False` في جلب عدة مصادر، بما فيها مسارات Azimut ومصدر الجلب العام.

**الأثر:** اتصال أقل أمانًا وإخفاء أخطاء الشهادات، مع احتمال قبول اتصال غير موثوق.  
**الإجراء المقترح:** إزالة `verify=False`. إذا كان المصدر يملك شهادة غير قياسية، يجب معالجة سلسلة الشهادات أو استخدام CA موثوق موثق، لا تعطيل التحقق عالميًا.

#### تحميل الواجهة بعدد كبير من الطلبات المتفرقة

صفحة الاقتصاد تجلب عددًا كبيرًا من السلاسل، وتستخدم pagination بحجم 1000. صفحة الصناديق تبدأ بأربع طلبات رئيسية، ثم تجلب بيانات الأداء، ثم تنفذ طلبًا مستقلًا لكل benchmark تقريبًا. هذا التصميم يعمل مع البيانات الحالية، لكنه يزيد زمن الانتظار وحساسية الصفحة للاتصال.

**الأثر:** ظهور حالة التحميل لفترة طويلة، وارتفاع احتمال الفشل الجزئي، واستهلاك غير ضروري لطلبات REST.  
**الإجراء المقترح:** إنشاء Views أو RPCs مخصصة للقراءات المركبة، وجلب ملخصات الصفحة في طلب واحد أو عدد محدود من الطلبات، وتحميل تفاصيل الرسوم عند فتحها فقط.

### أولويات متوسطة

#### JavaScript وCSS مضمّنان داخل الصفحات

`fund.html` يحتوي عدة كتل JavaScript مضمّنة، و`funds.html` و`macro.html` يحتويان منطقًا كبيرًا داخل HTML. كما توجد CSS كبيرة مضمّنة داخل صفحات متعددة، رغم وجود `app.css` و`header.css`.

**الأثر:** صعوبة الاختبار وإعادة الاستخدام، وتعارض محتمل بين قواعد الصفحة والقواعد المشتركة، وارتفاع تكلفة أي تعديل شامل.  
**الإجراء المقترح:** استخراج المنطق إلى وحدات، ثم تجميعه عبر Vite أو أداة build خفيفة. يجب أن يصبح HTML مسؤولًا عن الهيكل فقط.

#### الرأس والتنقل ليسا مكوّنًا حقيقيًا

يوجد تكرار لبنية `site-header` داخل صفحات متعددة. `header.css` يوحّد الشكل جزئيًا، لكنه لا يوحّد markup أو منطق تحديد الصفحة الحالية أو سلوك الهاتف.

**الأثر:** أي تغيير في التنقل يحتاج تعديل عدة صفحات، ويزيد احتمال اختلاف العنوان أو الصفحة النشطة أو سلوك الهاتف.

**الإجراء المقترح:** إنشاء مكوّن Header واحد، مع قائمة تنقل موحدة، وحالة active محسوبة من المسار، وتجربة هاتف واضحة.

#### لا توجد استراتيجية موحدة لحالات التحميل والخطأ والبيانات الفارغة

تستخدم الصفحات رسائل مختلفة مثل «جاري تحميل البيانات» و«تعذر تحميل البيانات»، وبعض المسارات لا تعرض زر إعادة محاولة أو تفاصيل مفيدة. تم تحسين صفحة الصناديق جزئيًا، لكن لا يوجد نظام مشترك.

**الأثر:** سلوك غير متسق ويصعب على المستخدم معرفة ما إذا كانت الصفحة تعمل أو تنتظر أو فشلت.

**الإجراء المقترح:** بناء مكونات أو وظائف موحدة لحالات `Loading`, `Error`, `Empty`, و`Stale data`، مع رسائل عربية ثابتة وإجراء واضح للمستخدم.

#### العقود بين الواجهة وقاعدة البيانات غير موثقة

الواجهة تعتمد مباشرة على أسماء حقول Supabase، مثل `final_score` و`report_date` و`data_confidence`، دون ملفات schema أو types أو توثيق مركزي.

**الأثر:** تغيير حقل في قاعدة البيانات قد يكسر صفحة دون اكتشاف مبكر.

**الإجراء المقترح:** تعريف TypeScript types أو JSON schema لعقود الجداول والـViews المستخدمة، وتسجيل الحقول المطلوبة والاختيارية وتواريخ التحديث.

#### بعض الحسابات تُنفذ في المتصفح بدل طبقة بيانات موحدة

تُحسب عوائد benchmark والتضخم والأذون داخل `funds.html` و`funds-p1.js`، بينما توجد أيضًا دالة SQL باسم `benchmark_return`. وجود الحساب في أكثر من موضع يجعل قواعد الحساب قابلة للاختلاف.

**الأثر:** احتمال اختلاف النتيجة بين صفحة الصندوق وقائمة الصناديق والدالة SQL.

**الإجراء المقترح:** تحديد مكان واحد للحسابات الرسمية. الحسابات التي تحمل معنى تحليليًا أو تصنيفيًا يجب أن تكون في قاعدة البيانات أو خدمة بيانات موحدة، بينما يقتصر المتصفح على العرض.

#### عدم اتساق العناوين والهوية في تاريخ المشروع

تم توحيد بعض العناوين مؤخرًا، لكن وجود تغييرات brand سابقة عبر workflows مؤقتة يكشف أن الهوية لم تكن مرتبطة بمصدر واحد. يجب اعتبار النصوص والعناوين جزءًا من نظام المحتوى لا تعديلات نصية متناثرة.

**الإجراء المقترح:** تعريف هوية الموقع وعناوين الصفحات ووصفها في إعداد مركزي أو مكوّن مشترك.

#### فحص الروابط الساكن الحالي يعطي إشارات كاذبة بسبب الروابط الديناميكية

الروابط مثل `fund.html?id=...` و`js/funds-p1.js?v=...` لا ينبغي فحصها كمسارات ملفات حرفية. هذا لا يثبت وجود عطل، لكنه يوضح أن أداة الفحص الحالية غير موجودة أصلًا وتحتاج إلى فهم الروابط الديناميكية.

**الإجراء المقترح:** إنشاء link checker يميز بين الموارد الساكنة، والروابط الداخلية، والروابط التي تبنى وقت التشغيل.

### أولويات منخفضة

#### إمكانية الوصول تحتاج إلى معيار واضح

أضيفت بعض `aria-label` في صفحة الصناديق، لكن التغطية ليست منهجية عبر الصفحات والرسوم والجداول. لا يوجد فحص آلي مثل axe أو Lighthouse.

**الإجراء المقترح:** إضافة أسماء واضحة للأزرار والرسوم، حالات focus مرئية، بنية عناوين صحيحة، دعم لوحة المفاتيح، واختبار آلي للوصولية.

#### إعدادات الهاتف تعتمد على التمرير الأفقي

التنقل الحالي قابل للتمرير على الهاتف، وهو حل عملي، لكنه لا يوضح دائمًا وجود عناصر إضافية ولا يوفر بديلًا منظّمًا عند كثرة الروابط.

**الإجراء المقترح:** تصميم mobile navigation واضح، مع قائمة قابلة للفتح أو شريط تمرير ذي مؤشرات، مع الحفاظ على الوصول بلوحة المفاتيح.

#### لا توجد مراقبة أداء أو مؤشرات تشغيل

لا يتم قياس زمن تحميل الصفحة أو عدد الطلبات أو زمن استجابة Supabase داخل المشروع.

**الإجراء المقترح:** إضافة قياسات بسيطة في بيئة الإنتاج، مثل زمن أول عرض، زمن اكتمال البيانات، عدد الطلبات، ونسبة فشل التحميل، دون تسجيل بيانات حساسة.

#### المتطلبات موصوفة بشكل مختصر

`README.md` يشرح الجدولة والأسرار وقاعدة التحويل، لكنه لا يشرح بنية الواجهة، عقود البيانات، طريقة التشغيل المحلي، أو خطة الاختبار.

**الإجراء المقترح:** توسيع README إلى دليل تشغيل وصيانة، مع قسم architecture وdata contracts وrelease checklist.

#### توجد آثار بيئة محلية غير مناسبة داخل مجلد العمل

ظهر مجلد `scripts/__pycache__` أثناء الفحص المحلي. وهو مستبعد عبر `.gitignore`، لكنه يوسّخ بيئة العمل ويجب تنظيفه قبل أي أرشفة أو package عملية.

## حالة التشغيل التي تم التحقق منها

تم التحقق من اتصال REST العام بالمشروع المرتبط:

| الكيان | نتيجة القراءة |
|---|---:|
| `funds` | 206 سجلًا تقريبًا |
| `nav_official` | 185 سجلًا تقريبًا |
| `macro_series` | 26,922 سجلًا تقريبًا |
| `fund_performance_history` | 52,386 سجلًا تقريبًا |
| `fund_smartscore_latest` | 206 سجلًا تقريبًا |
| `smartscore_evaluations` | 11,760 سجلًا تقريبًا |
| `smartscore_methodology_versions` | سجلان |

أحدث البيانات التي ظهرت أثناء الفحص كانت NAV حتى **2026-09-03**، وسلاسل الاقتصاد الكلي حتى **2026-09-02**، بينما كان أحدث أداء وSmartScore حتى **2026-07-30**. هذا الفرق ليس بالضرورة خطأ، لكنه يجب أن يظهر للمستخدم بوضوح على مستوى كل بطاقة وصفحة.

تم فحص الواجهة الحية. الصفحة الرئيسية وصفحة الاقتصاد تعملان وتعرضان بيانات Supabase. صفحة الصناديق تصل إلى 206 سجلًا وتعرض النتائج بعد اكتمال الطلبات؛ أما حالة التحميل الطويلة التي ظهرت سابقًا فكانت مرتبطة بحلقة مراقب النتائج، وقد تم إصلاحها قبل هذا التدقيق. لا يعني ذلك أن المسار أصبح محميًا باختبارات مستقبلية، لأن اختبارات المتصفح غير موجودة بعد.

## خطة إعادة البناء المقترحة

### المرحلة 0: تثبيت المنتج الحالي

ينبغي أولًا تنفيذ إصلاحات لا تعتمد على إعادة بناء الواجهة: حذف أو تنفيذ `heatmap.html`، إضافة فحص قبل النشر، إزالة `verify=False`، توثيق عقود Supabase، وتثبيت مصدر بيانات واحد لكل نوع من المعلومات.

### المرحلة 1: تأسيس النظام المشترك

يُنقل الرأس والتنقل والتذييل إلى مكوّن مشترك. بعد ذلك تُبنى طبقة Supabase واحدة تتولى الطلبات، pagination، الأخطاء، والكاش. في هذه المرحلة يجب أيضًا إنشاء نظام tokens للتصميم ومكونات حالات التحميل والخطأ والفراغ.

### المرحلة 2: نقل الصفحات ذات القيمة الأعلى

تُنقل الصفحة الرئيسية وصفحة الاقتصاد وصفحة الصناديق إلى بنية مكونات قابلة للاختبار. تكون الأولوية لصفحة الصناديق لأنها تجمع البحث والفلاتر والفرز والبيانات الزمنية والتقييم.

### المرحلة 3: نقل ملف الصندوق وخريطة الفئات

بعد ثبات الطبقة المشتركة، يُنقل `fund.html` وتبويباته، ثم تُنفذ خريطة الفئات وخريطة الحرارة باعتبارهما تجربة واحدة لا صفحتين متنافستين.

### المرحلة 4: الإنتاجية والقياس

يُضاف build production، وفحص الروابط، واختبار متصفح، وفحص وصولية، وقياس أداء. لا يُسمح بالنشر إذا فشل أي فحص أساسي.

## Backlog مرتب بالأولوية

| الترتيب | المهمة | الأولوية | القيمة |
|---:|---|---|---|
| 1 | تنفيذ أو إزالة `heatmap.html` | حرجة | إغلاق الصفحة الفارغة |
| 2 | إضافة CI quality gate قبل Pages deploy | عالية | منع نشر الأعطال |
| 3 | اعتماد مصدر حقيقة واحد للبيانات | عالية | منع اختلاف الصفحات |
| 4 | إزالة تعطيل TLS في ingest NAV | عالية | رفع أمان الإدخال |
| 5 | بناء طبقة Supabase مشتركة | عالية | تقليل الطلبات والتكرار |
| 6 | إضافة اختبارات unit/integration/browser | عالية | حماية من رجوع الأخطاء |
| 7 | استخراج Header وDesign System | متوسطة | اتساق شامل |
| 8 | نقل JavaScript وCSS خارج HTML | متوسطة | قابلية الصيانة |
| 9 | توحيد حالات loading/error/empty/stale | متوسطة | تجربة أوضح |
| 10 | تحسين الهاتف والوصولية والقياس | متوسطة | احترافية المنتج |

## القرار المعماري المقترح

الانتقال إلى **React + Vite أو بنية مكونات static مماثلة** مناسب للمشروع، بشرط أن يكون انتقالًا تدريجيًا لا إعادة كتابة عمياء. يجب أن تبقى GitHub Pages طبقة النشر، وأن تبقى Supabase مصدر البيانات، وأن تُنقل صفحة واحدة في كل مرة مع مقارنة النسخة القديمة والجديدة واختبار مساراتها.

إذا كان الهدف تقليل المخاطر والوقت، يمكن البدء بمرحلة استخراج وحدات JavaScript وCSS وطبقة API دون تغيير كامل إلى React. أما إذا كان الهدف منصة قابلة للتوسع لعدة سنوات، فالأفضل اعتماد React/Vite مبكرًا بعد تثبيت عقود البيانات.

## خلاصة القرار

المنصة ليست بحاجة إلى المزيد من الترقيعات الموضعية. هي بحاجة إلى **تثبيت الوظائف المفقودة، ثم تأسيس طبقة مشتركة للبيانات والتصميم والاختبار، ثم إعادة نقل الصفحات تدريجيًا**. هذا المسار يعالج السبب الجذري للمشكلات الحالية ويقلل احتمالية عودة أخطاء مثل الصفحة الفارغة، توقف التحميل، اختلاف البيانات، وفشل النشر الصامت.

## المراجع

[1]: https://github.com/Khater1984/Khater-data "مستودع مشروع Khater-data على GitHub"

[2]: https://khater1984.github.io/Khater-data/ "الواجهة المنشورة لمنصة رادار السوق المصري"

[3]: https://jlaqotegkeszuyqzdham.supabase.co "مشروع Supabase المرتبط بالمنصة"
